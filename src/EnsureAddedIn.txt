camera.cpp
    add peranti_camera_sync(cameraPosX, cameraPosY, cameraPosZ, cameraDirX, cameraDirY cameraDirZ, fovX, fovY, nearVal, farVal, offsetX, offsetY, offsetZ)

    (all types are float, other than offset which is int)
client.cpp

    typedef struct {
        const float *positions;   // 3 floats per vertex
        const float *normals;     // 3 floats per vertex
        const float *uvs;         // 2 floats per vertex
        const uint32_t *indices;
        size_t vertex_count;
        size_t index_count;
    } PerantiRawMesh;
    add peranti_extract_mapblock_mesh(int16_t mapblockX, int16_t mapblockY, int16_t mapblockZ, const PerantiRawMesh *mesh_data)
